<form action="{{ path('fos_user_group_edit', {'groupname': groupname} ) }}" {{ form_enctype(form) }} method="POST" class="fos_user_group_edit">
    <legend>{{ 'group.edit.form'|trans }}</legend>
    {{ form_widget(form, {'form_type': 'horizontal'} ) }}
    <div class="form-actions">
        <input class="btn btn-primary" type="submit" value="{{ 'group.edit.submit'|trans([], 'FOSUserBundle') }}" />
    </div>
</form>
